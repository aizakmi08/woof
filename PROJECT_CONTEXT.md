# Woof Project Context

Last updated: 2026-06-17.

Woof is a React Native/Expo pet food scanner app in `/Users/admin/Documents/woof`, backed by Supabase, Anthropic Claude through a Supabase Edge Function, Open Pet Food Facts, and RevenueCat subscriptions. The live App Store app id is `6760733899`.

## Current Audit State

- The repo has been connected to `https://github.com/aizakmi08/woof.git` on branch `main`.
- `AUDIT.md` is the current source-backed audit report.
- `PRODUCT_UX_REVENUE_AUDIT.md` is the current product-flow and monetization UX audit for onboarding, Home, scanner, Results, paywall, Profile, account saving, and retention experiments.
- Key live-dashboard findings:
  - GitHub release path on June 17, 2026: remote `main` is `f1ecad5c47c644a512470e9515c0ffbe8d45edff`; remote `codex/push-woof-app` is `b6626b1296af168ddd6f08c451751ae0a2ada8d2`; PR #1 is open and draft, has 0 statuses/check runs, and is a different/stale release branch relative to the current local audit worktree. GitHub Actions currently exposes only the Pages deployment workflow; the smoke CI workflow added locally has not been pushed or run on GitHub. PR #1 also carries stale `supabase/migrations/007`-`049` files that collide with production's existing `007`-`057` Supabase migration history. See `GITHUB_RELEASE_AUDIT.md`.
  - App Store Connect 90-day overview ending June 15, 2026: 239 first-time downloads, 6 redownloads, 4.64K impressions, 509 product page views, 6.79% App Store conversion rate daily average, `$43` proceeds, 19 in-app purchases, 4 active subscription plans, 3 paid plans, `$11` MRR, Day 7 download-to-paid 1.44%, Day 35 download-to-paid 1.92%, 1 churned plan, and 2 crashes on version 1.2 iOS.
  - App Store Connect Distribution for iOS version 1.2 is `Ready for Distribution`, has five iPhone screenshots, blank Promotional Text, unsupported DogFoodAdvisor/CatFoodAdvisor copy in Description, unsupported `recall alerts` language in App Review Notes, automatic release after review enabled, and build row `31` / build version `1.1.1`; `APP_STORE_CONNECT_AUDIT.md` records the read-only evidence without private contact fields.
  - RevenueCat last 28 days: 118 new customers, 3 active subscriptions, 1 active trial, `$21` MRR, `$18` revenue.
  - RevenueCat has one active `default` offering with weekly, monthly, and annual packages. The current offering page shows `Add Paywall` and `Add Rules`, so no hosted paywall or targeting rule is attached to that offering. RevenueCat overview, paywalls, and experiments routes were stuck on `Loading RevenueCat...` during the June 17 Chrome pass, so broader metrics and experiment status still need a fresh dashboard recheck.
  - Supabase current billing-cycle egress is `7.519 / 5 GB (150%)` for May 27-June 27, 2026, with `7.52 GB` used, `2.52 GB` overage, only `568 / 500,000` Edge Function invocations, `8 / 50,000` monthly active users, and a grace period ending July 15, 2026.
  - Supabase dashboard has deployed functions `analyze`, `product-lookup`, and `revenuecat-webhook`; those three now have tracked source in this repo, but the recreated `product-lookup` and `revenuecat-webhook` need to be compared against/deployed over the live dashboard versions. The worktree also now includes a new `revenuecat-sync` function that must be deployed before the next build relies on immediate purchase/restore entitlement sync.
  - App Store listing publicly claims no account required and verified DogFoodAdvisor/CatFoodAdvisor data. The current worktree now implements a Supabase anonymous-auth path for no-account scanning, but the live build still needs Supabase dashboard enablement, TestFlight validation, and deployment. DogFoodAdvisor/CatFoodAdvisor support is still not present in this repo.

## Fixes Already Applied

- Ignored and removed tracked `supabase/.temp/*` files.
- Fixed hosted website App Store link to `https://apps.apple.com/app/id6760733899`.
- Updated hosted privacy page to reflect human-food scans, subscriptions, OPFF, operational logs, and 2026 date.
- Added Supabase migration `058_human_food_history.sql` for `scan_history.safety_level`.
- Live Supabase reconciliation on June 17, 2026 found production project `rhlgvrywjralxrjcdtrw` already has remote migration history through `057_tighten_supplemental_catalog_exclusions`. Local audit migrations are now deployable as `058`-`070`, and `SUPABASE_LIVE_RECONCILIATION.md` records the live migration/table/function/advisor evidence plus the migration-history repair caveat.
- Updated `services/history.js` to persist/read `safetyLevel`.
- Fixed human-food local cache shape in `services/analysisService.js`.
- Added `SECURITY DEFINER` cache-hit RPC migration and updated the original cache migration for new setups.
- Added `services/analytics.js`, Supabase migration `060_analytics_events.sql`, and funnel tracking across onboarding, auth, scanner, results, paywall, purchases, restore, history, profile, and sharing. Analytics events now include centralized release context (`app_version`, `native_build_version`, `runtime_version`, platform, and execution environment) so TestFlight and production issues can be segmented by build.
- Hardened analytics queue attribution in the worktree: queued events now record the user id they were captured under, flush only when they are pre-auth or match the active user, flush on both `SIGNED_IN` and cold start with an existing Supabase session, and use a single-flight lock to avoid duplicate inserts. `analytics_queue_flushed` records recovered event volume, `analytics_queue_dropped` records legacy/user-mismatch drops, and `auth_signed_out` is not queued for attribution to a future user.
- Added `KPI_FRAMEWORK.md` and Supabase migration `065_kpi_reporting_views.sql` for daily funnel, activation cohort, first-run auth entry diagnostics, release/build diagnostics, scan-failure diagnostics, share diagnostics, paywall request/source/package-load/variant/pitch/plan conversion, scan usage, RevenueCat webhook/subscriber-sync health, and event-count reporting views.
- Added `WEEKLY_REVIEW_RUNBOOK.md` as the weekly operating review: dashboard inputs to refresh, paste-ready KPI SQL, red/yellow decision rules, and a narrative template for product/revenue/reliability/growth actions.
- Added `REVENUECAT_EXPERIMENT_PLAN.md` for the RevenueCat baseline, paywall pitch, monthly-vs-annual, placement/source, dashboard, and stop-condition experiment loop.
- Added `scripts/check-revenuecat-readiness.mjs`, `npm run check:revenuecat`, CI coverage, and release-preflight coverage so the RevenueCat release contract is now explicit: safe Offering Metadata allowlists, weekly/monthly/annual package telemetry, store-price/trial truthfulness, purchase/restore entitlement refreshes, immediate `revenuecat-sync` diagnostics, KPI definitions, and weekly stop conditions must stay aligned before shipping or running paywall experiments.
- Added `REVENUECAT_TESTFLIGHT_RUNBOOK.md` and wired it into `npm run check:revenuecat` so RevenueCat readiness now requires a concrete TestFlight evidence path for offering/package load, trial eligibility, purchase, restore, cancellation/expiration, guest account saving, webhook subscriber sync, KPI SQL review, and `RELEASE_EVIDENCE.md` handoff before experiments or paid growth.
- Added `APP_STORE_LISTING.md` with paste-ready App Store metadata, screenshot captions, ASO keyword guidance, Product Page Optimization ideas, App Review notes, and unsupported-claim guardrails. `scripts/check-app-store-listing.mjs` / `npm run check:listing` validates the recommended metadata against App Store field limits, unsafe claims, required AI/veterinary disclaimers, and guest-scan release gates.
- Hardened the App Store screenshot plan in the worktree: the first three screenshots are now explicitly product-proof-first (scan, score, ingredient flags), screenshot captions are capped for search-result readability, screenshot 5's `3 free scans` copy stays gated on anonymous guest scan validation, and `npm run check:listing` now verifies screenshot order/captions plus TestFlight-derived production rules.
- Added `scripts/check-live-app-store-listing.mjs` / `npm run check:live-listing` to verify the public US App Store description after App Store Connect metadata changes. The current live listing still intentionally fails the clean mode because it contains unsupported DogFoodAdvisor/CatFoodAdvisor/verified-data claims, overbroad safety language, and an unvalidated no-account promise; use `--expect-current-risk` until the replacement listing is visible, then `--guest-validated` before release.
- Added `APP_STORE_CONNECT_AUDIT.md` with the current 90-day App Store Connect KPI baseline, live iOS 1.2 Distribution metadata risks, RevenueCat offering snapshot, and operating recommendations for trust cleanup before paid acquisition.
- Added `GITHUB_RELEASE_AUDIT.md` with the current remote branch/PR/Actions evidence, including the stale draft PR #1 caveat, missing remote smoke CI, unprotected branches, PR #1's `007`-`049` migration collision with production Supabase history, and the recommended safe publish path.
- Added `.github/pull_request_template.md` and GitHub release-audit guard coverage so future release PRs must surface the stale PR #1 warning, stale `supabase/migrations/007`-`057` collision risk, required local validation commands, strict release-evidence gate, live Edge verifier, App Store live-listing check, TestFlight/RevenueCat/Supabase proof, and paid-growth pause before merge.
- Added `APP_PRIVACY_DISCLOSURE.md` with source-backed App Store Connect App Privacy answers for Woof's current Supabase, Anthropic, Open Pet Food Facts, RevenueCat, analytics, scan-history, support, and Sentry data flows. `scripts/check-app-privacy-disclosure.mjs` / `npm run check:privacy` validates the disclosure pack, third-party processor inventory, no-tracking/no-IDFA assumptions, linked-data categories, hosted privacy policy alignment, and Sentry Crash Data status.
- Added `GROWTH_CREATIVE_PLAN.md` with launch gates for paid spend, first audience segments, safe ad hooks, Search Ads/social/organic test sequencing, KPI decision rules, and a 30-day growth roadmap.
- Added Creative Production Ads Explorer prompt artifacts under `outputs/imagegen/woof-digital-product-ads/` using the `digital-product-core-ad-prompts` pack. No images were generated yet; the folder contains prompt manifests, jobs, metadata, and a static review fallback for later screenshot-grounded asset generation.
- Added `PRODUCT_UX_REVENUE_AUDIT.md` with a source-backed scan-first funnel review, first-run/onboarding gaps, empty-state CTA recommendation, guest account-save timing recommendation, human-food copy gap, source-specific paywall testing guidance, and retention experiment backlog.
- Implemented the first product UX fixes from `PRODUCT_UX_REVENUE_AUDIT.md`: `AuthProvider` now mounts while onboarding is visible, the first onboarding screen has a `Scan Product` path that routes to Scanner after anonymous auth readiness and reinforces no-account scanning, 3 free scans, pet-food labels, human-food checks, and save-later account creation; Home empty history includes inline pet-food and human-food scan actions with `source_surface: "home_empty_state"`, Results has a contextual, user-scoped guest-save prompt after successful anonymous scans, and Scanner human-food capture/help copy uses food-safety language.
- Added onboarding path KPI segmentation in the worktree: `public.kpi_onboarding_path_daily` compares `scan_now`, `completed_flow`, `scan_now_incomplete`, `education_incomplete`, and `started_only` paths through onboarding completion, auth exposure/completion, scan start/completion/failure, paywall view, and purchase completion so the new scan-first path can be judged against the education path.
- Added scanner capture-tip guidance in the worktree: Scanner now shows a second concise tip line under the primary instruction, uses mode/fallback-specific help copy, and emits `scanner_help_opened` with the displayed `capture_tip`. `065_kpi_reporting_views.sql`, `KPI_FRAMEWORK.md`, and `WEEKLY_REVIEW_RUNBOOK.md` expose `scanner_help_opens` so first-scan guidance demand can be reviewed beside camera permission and capture-completion metrics.
- Added Home free-scan quota clarity in the worktree: free users see a compact remaining-scan status nudge after the primary scan CTAs, Home scan CTA events now include `remaining_scans`, and `free_scan_status_tapped` routes to the existing `home_banner` paywall source with `source_surface: "home_free_scan_status"`. `065_kpi_reporting_views.sql`, `KPI_FRAMEWORK.md`, and `WEEKLY_REVIEW_RUNBOOK.md` now expose `free_scan_status_taps` so quota clarity can be reviewed against scan-limit paywall requests.
- Added the first lightweight retention surface in the worktree: Home now shows a `Compare recent scans` card after two scored pet-food history items, opens a side-by-side comparison modal, and tracks `history_compare_opened`, `history_compare_closed`, `history_compare_result_opened`, and source-attributed history opens. `065_kpi_reporting_views.sql` now exposes this through `public.kpi_daily_funnel`, `public.kpi_activation_cohorts`, and `public.kpi_retention_daily`.
- Expanded Home history into a measurable repeat-shopping tool: users can search saved scans, filter pet-food versus human-food history, clear filters, and expand beyond the default 10-row recent list. Analytics now track history search, filter, expand/collapse, and search/filter result opens, and `065_kpi_reporting_views.sql`, `KPI_FRAMEWORK.md`, and `WEEKLY_REVIEW_RUNBOOK.md` expose `history_tool_result_open_rate` alongside the compare metrics.
- Added anonymous-first auth in the worktree: automatic Supabase guest sessions, a fallback "Continue as Guest" button, guest-aware profile UI, account-saving through Apple/Google identity linking, explicit guest-history migration if linking changes the user id, and guest-aligned legal copy. `kpi_daily_funnel` now separates auth-screen fallback exposure, automatic guest session starts/completions/failures, manual guest continuation, and Apple/Google provider sign-in events so first-run auth friction is measurable instead of collapsed into one auth counter. The release checklist now also requires the Supabase Auth dashboard decisions that anonymous auth depends on: manual linking, Apple/Google redirect URLs, anonymous-user `authenticated` role/RLS review with `is_anonymous`, abuse/rate-limit controls, anonymous-user cleanup retention, and Leaked Password Protection or a documented waiver.
- Hardened Supabase OAuth callback completion in the worktree: Apple/Google browser auth now exchanges PKCE `code` callbacks through `exchangeCodeForSession`, still supports implicit `access_token` / `refresh_token` callbacks, and fails explicitly when no session material returns.
- Added server-side free-scan enforcement in the worktree: `061_scan_entitlements.sql`, `services/entitlements.js`, scan usage wiring in analysis/results/auth, and an updated `analyze` Edge Function that gates Claude calls through the idempotent `consume_scan` RPC.
- Hardened first-run scan count state in the worktree: the app no longer loads the legacy global `@woof_scan_count` before a user is known, and persisted scan counts are now scoped by Supabase user id. This prevents a previous guest/account's local free-scan count from falsely blocking a new anonymous session before the server-side scan gate checks the real profile.
- Added tier-aware analyze abuse throttling in the worktree: `068_tiered_rate_limits.sql` updates `check_rate_limit` so free users keep the 20/hour abuse cap while active Pro users get a higher operational safety cap, and the `analyze` 429 message no longer incorrectly says every user is capped at 20 scans per hour.
- Added profile write hardening in the worktree: `066_profile_write_security.sql` prevents authenticated clients from directly mutating `profiles.is_pro`, `scan_count`, `pro_expires_at`, or RevenueCat fields; `services/auth.js` no longer writes Pro state or scan count directly; `scan_history` now has an own-row update policy for upsert-based sync.
- Added delete-account privacy hardening in the worktree: `067_delete_account_privacy.sql` removes linkable analytics, RevenueCat webhook, scan usage, and rate-limit rows before deleting the auth user; the RPC explicitly revokes default `PUBLIC`/`anon` execution and grants only `authenticated`; the client delete flow clears local scan history, local result cache, scan count, and analytics queue/session storage.
- Added scheduled cleanup in the worktree: `064_schedule_cleanup_jobs.sql` enables Supabase Cron/`pg_cron`, hardens cleanup functions, and schedules expired cache plus stale rate-limit cleanup.
- Added tracked RevenueCat entitlement sync in the worktree: `062_revenuecat_webhook.sql`, `supabase/functions/revenuecat-webhook/index.ts`, raw webhook event logging, profile Pro-state updates, authorization-header verification, optional RevenueCat `GET /subscribers` sync through `REVENUECAT_REST_API_KEY`, `TRANSFER` source/destination handling, and RevenueCat client config/user-switching hardening.
- Added immediate post-purchase RevenueCat profile sync in the worktree: `supabase/functions/revenuecat-sync/index.ts` validates the authenticated Supabase user, fetches the user's RevenueCat subscriber record with `REVENUECAT_REST_API_KEY`, and service-upserts `profiles.is_pro` / `pro_expires_at` / RevenueCat metadata. `services/revenuecatSync.js` and `services/auth.js` call it after purchase, restore, and Pro-state refresh so the server-side scan gate is not dependent only on webhook timing.
- Hardened RevenueCat sync diagnostics in the worktree: when the `revenuecat-sync` Edge Function returns an HTTP error, `services/revenuecatSync.js` now extracts the function response body and tracks `http_status`, `sync_status`, and `function_error` on `revenuecat_profile_sync_failed`, making missing/invalid `REVENUECAT_REST_API_KEY` or subscriber fetch failures actionable in analytics.
- Hardened paid-user entitlement precedence in the worktree: if the RevenueCat SDK reports an active Pro entitlement but immediate server subscriber sync returns `is_pro=false`, the app keeps local Pro access, emits `revenuecat_status_mismatch`, and starts one deduplicated three-attempt server reconciliation with bounded backoff. The reconciliation never writes a client entitlement claim; every attempt calls the authenticated `revenuecat-sync` function, stops as soon as the server confirms Pro, and records started/attempt/completed/exhausted diagnostics. The paywall records `purchase_entitlement_refreshed` / `restore_entitlement_refreshed` so TestFlight can prove whether purchase and restore refreshes actually resolve to Pro. The purchase and restore paths include safe RevenueCat result diagnostics (`error_code`, active entitlement/subscription counts, and product/entitlement ids), track `purchase_pending`, distinguish `purchase_no_entitlement` from SDK failures, and distinguish `restore_no_entitlement` from a true no-purchase restore. Profile exposes a real "Restore Purchases" action that initializes RevenueCat for the active user, calls restore, refreshes Pro state, and emits the same restore outcome events promised by the support page.
- Hardened expiration reconciliation: when the RevenueCat SDK has completed its check with no active entitlement, Woof returns Free immediately for normal free profiles. If Supabase still carries an active Pro profile, Woof records the mismatch and invokes authenticated `revenuecat-sync` before deciding access, allowing server truth to revoke stale Pro or preserve legitimate server-confirmed access without trusting the client.
- Added StoreKit renewal-rate release guards: `check:revenuecat` rejects accelerated timing in both the committed and generated catalogs, and `check:prebuild` verifies a clean Expo prebuild still copies a Real Time catalog. This prevents a temporary expiry-test rate from leaking into normal simulator runs.
- Added an explicit StoreKitTest expiration helper for `woof_pro_monthly`. It is skipped unless `WOOF_STOREKIT_EXPIRE_MONTHLY=1` is supplied, uses Apple's `expireSubscription` API, disables dialogs, and leaves ordinary CI/catalog tests side-effect free.
- Hardened subscription support in Profile and paywall copy: the Manage Subscription action now awaits the App Store / Google Play subscription link, emits `subscription_manage_opened` or `subscription_manage_failed`, and shows fallback cancellation guidance if the store settings page cannot open. Restore and store-error copy now references App Store / Google Play where the flow is cross-platform, and the hosted support FAQ now gives both iPhone and Android cancellation paths.
- Hardened RevenueCat initialization before paywall display in the worktree: `PaywallScreen` now calls `initializePurchases(user.id)` before fetching offerings, so a transient auth-startup initialization miss is retried at the revenue moment. `resetPurchases()` now waits for in-flight RevenueCat logout/reset before another user is logged into the SDK. `paywall_offerings_loaded` also records expected weekly/monthly/annual availability and missing plan counts so dashboard/package setup issues can be separated from weak paywall copy.
- Hardened guest account-linking revenue state in the worktree: after a guest saves an account with Apple or Google, `finishAccountLink` explicitly calls `initializePurchases(updatedUser.id)` and tracks `account_link_revenuecat_reidentified` before running the account-link Pro refresh, so RevenueCat is re-identified to the active Supabase user before entitlement sync. The Pro refresh now accepts an explicit `userId` and account-link/Google sign-in flows pass `updatedUser.id`, avoiding stale guest/null profile fallback if React state has not caught up yet.
- Changed the in-app paywall to the tracked `monthly_default_v1` variant: Monthly is selected by default, trial copy is shown only when the loaded RevenueCat package has a claimable free trial, paywall copy adapts by entry source, paywall/purchase analytics include source/variant/pitch/plan/trial/package-availability metadata, and the unsupported review/recall feature claim was removed.
- Added placement-aware RevenueCat offering fetch in the worktree: the paywall now maps `results_gate`, `scan_limit`, `post_scan_prompt`, `home_banner`, and `profile` to matching RevenueCat placement identifiers through `getCurrentOfferingForPlacement`, keeps `offerings.current` as the fallback when a placement is unconfigured or returns no offering, and records `placement_identifier`, `placement_requested`, `placement_supported`, `offering_fetch_mode`, `placement_offering_returned`, and `placement_fallback_used` on paywall/purchase/restore/dismissal analytics. Development builds now include an expandable paywall RevenueCat Debug panel guarded by `__DEV__`, showing configured key status, platform, source, placement id, fetch mode, placement support/return/fallback state, offering id, package/product ids, prices, selected variant/default plan/pitch, and accepted/ignored metadata signals. `public.kpi_paywall_source_daily` now exposes placement request, return, fallback, error, return-rate, and fallback-rate metrics so source-specific offering tests can be debugged before judging paywall copy or pricing.
- Added source-intent paywall segmentation in the worktree: Paywall now shows a compact context strip for result details, scan continuity, compare-more, shopping, profile upgrade, or general upgrade jobs, and emits `source_intent` plus `source_context_label` across view, offering, metadata, trial, plan, purchase, restore, close, and dismissal analytics. `public.kpi_paywall_source_daily`, `public.kpi_paywall_pitch_daily`, and `public.kpi_paywall_daily` include `source_intent` so source-specific paywall tests can stay interpretable if RevenueCat placements or navigation source names change.
- Scoped result prompt state in the worktree: first-scan toast and post-scan upgrade prompt flags now belong to the current guest/account, post-scan prompt analytics include `prompt_state_scoped`, and delete-account clears local result prompt, guest-save prompt, and review prompt state.
- Added development-only local web QA for paywall sources in the worktree: `App.js` supports `?woof_paywall_preview=<source>` for `results_gate`, `scan_limit`, `post_scan_prompt`, `home_banner`, and `profile`, skips automatic guest-session creation for that preview route, and renders `PaywallScreen` with a mock scanned product so the RevenueCat Debug panel can be inspected without a device, purchase, or Supabase auth call. Production builds stay guarded by `__DEV__`.
- Added richer paywall close/dismiss telemetry in the worktree: close-button and native back/gesture exits emit one `paywall_dismissed` event with source, variant, pitch, selected plan, package-load state, trial fields, `exit_reason`, `close_outcome`, and `duration_ms`; purchase/restore success exits emit `paywall_closed` without inflating dismissals. KPI views expose `paywall_dismissals`, `paywall_view_dismissal_rate`, and `avg_paywall_dismiss_duration_ms` by source, pitch, variant, and plan.
- Standardized paywall request analytics in the worktree: Home scan-limit blocks, the home upgrade banner, Profile upgrade, and Results upgrade gates now emit `paywall_requested` before navigation. `065_kpi_reporting_views.sql` exposes `paywall_requests`, `scan_limit_paywall_requests`, `paywall_request_to_view_rate`, package-load counts/failures/rates, and a source-level `kpi_paywall_source_daily` view before drilling into pitch/plan conversion.
- Hardened paywall and upgrade-gate price/trial truthfulness in the worktree: weekly monthly-equivalent, annual anchor price, annual monthly-equivalent, and savings percent are computed from RevenueCat package prices instead of hard-coded claims; missing package prices render as loading/unavailable instead of fallback dollar amounts; trial CTA, timeline, accessibility labels, and disclosure are driven by RevenueCat product data plus iOS intro-eligibility status, with unknown/ineligible iOS results falling back to non-trial copy. The result-screen Pro gate no longer hard-codes `From $3.33/month`; secondary upgrade gates stay price-neutral until the RevenueCat-backed paywall loads current store prices.
- Added safe RevenueCat Offering Metadata support in the paywall for remote `paywall_variant`, `default_plan`, headline, and positioning hints. Remote copy is length-limited and rejected if it contains unsupported DogFoodAdvisor/CatFoodAdvisor, customer-review, recall, veterinary-approval, guaranteed-safety, or medical-diagnosis claims.
- Changed result sharing into a free branded acquisition loop instead of a Pro-only gate; share analytics now segment free/pro users, scan mode, data source, completion/dismissal/failure method, and whether an install/share URL was attached. Dismissed iOS share sheets emit `share_dismissed` instead of inflating `share_completed`.
- Added a configurable `WOOF_SHARE_URL` with a live App Store fallback (`https://apps.apple.com/app/id6760733899`). Result shares now append the link to text shares and render a compact "Get Woof" line inside the captured share card so image-only shares still point recipients back to the app.
- Added a conservative app-review loop in the worktree: Profile has a "Rate Woof" row, Results can show a cooldown-limited, user-scoped rating prompt only after repeat successful good-scoring pet-food scans, delete-account clears local review prompt state, and `065_kpi_reporting_views.sql` now exposes daily review prompt/request/open metrics plus `kpi_app_review_daily`.
- Added a measured Profile support loop in the worktree: "Contact Support" opens a prefilled email to `woofapp.help@gmail.com` with safe app/platform/account diagnostics, falls back to the support email if mail cannot open, emits `support_contact_tapped` / `support_contact_opened` / `support_contact_failed`, and exposes daily support diagnostics through `public.kpi_support_daily`.
- Made Profile scrollable and flex-safe in the worktree so guest account-saving, subscription/restore, rating, support, legal, sign-out, delete, and version rows remain reachable on smaller phones and long subscription/support subtitles cannot collide with chevrons. `scripts/check-js-syntax.mjs` now guards the Profile scroll layout marker.
- Removed unsupported customer-review and recall-history product claims from Claude prompts, result rendering, Pro gate copy, website support/home copy, hosted terms, and in-app legal terms. The validators now normalize `customerRating` to `null` and `recallHistory` to an empty string for pet-food results.
- Added tracked product lookup source in the worktree: `supabase/functions/product-lookup/index.ts`, plus `services/opff.js` now prefers the Edge Function and falls back to direct Open Pet Food Facts calls.
- Added adaptive scan-photo optimization in `ScannerScreen` to shrink unusually large image payloads before analysis, block oversized client payloads, and log optimization metadata for egress monitoring. `services/claude.js` now caps image streaming retries, suppresses automatic non-streaming fallback after an image streaming upload has already gone out, and emits `analysis_upload_started` plus `analysis_image_retry_suppressed` with estimated request/decoded-image bytes and the generated `scan_id`; `ResultsScreen` now sends the same `scan_id` through scan-start, completion, failure, timeout, cancellation, and scan-limit recovery retry events. `065_kpi_reporting_views.sql` includes upload-attempt, suppressed-image-retry, estimated-byte, cached/fresh scan completion, cache-rate, aggregate per-fresh-scan upload pressure, optimized capture-size averages (`avg_photo_capture_base64_length`, `avg_photo_capture_estimated_decoded_bytes`, `avg_photo_capture_optimization_step`, `avg_photo_capture_target_width`), and scan-id-matched upload-cost fields such as `matched_upload_bytes_per_completed_scan` and `matched_upload_bytes_per_fresh_scan`.
- Added stage-specific capture failure telemetry in `ScannerScreen`: `photo_capture_failed` now records `capture_stage` for camera capture, missing photo URI, image optimization, and missing optimized base64 failures. `065_kpi_reporting_views.sql` exposes `photo_capture_failures`, `camera_capture_failures`, and `image_optimization_failures` so first-scan leaks can be separated from upload/cost problems.
- Added `public.kpi_analysis_cache_health` so the weekly operating review can inspect active/expired shared cache rows, cache payload bytes, hit coverage, and active hits per cache row before deciding whether to shorten retention, prune stale rows, or keep cache growth because it is reducing fresh uploads and Claude calls.
- Hardened scanner cancellation before upload: tapping Cancel while the camera capture/optimization promise is still running now invalidates that capture, emits `photo_capture_cancelled`, and prevents the stale capture from navigating into Results or uploading to `analyze`. `065_kpi_reporting_views.sql` counts `photo_capture_cancellations` next to upload attempts and oversized-photo blocks.
- Hardened camera permission UX in `ScannerScreen`: permission copy is now mode-aware for pet-food labels versus human-food checks, the CTA says "Allow Camera" while the app can still request permission and "Open Settings" after permission is blocked, permission outcomes emit `camera_permission_result`, and settings fallback emits `camera_permission_settings_opened` / `camera_permission_settings_failed`. `065_kpi_reporting_views.sql` now exposes camera permission requests, grants, and denials.
- Lowered the `analyze` Edge Function image cap from `7_000_000` to `2_400_000` base64 characters so older or direct clients cannot forward oversized images to Claude and inflate Supabase egress. `scripts/check-edge-functions.mjs` now checks that the Edge cap stays aligned with `ScannerScreen`'s client cap.
- Aligned local release metadata to the live 1.2 line: `app.json`, `package.json`, and `package-lock.json` now use `1.2.0`. Because `eas.json` uses `appVersionSource: "remote"`, `EAS_RELEASE_VERSIONING.md` and `scripts/check-eas-versioning.mjs` now require the pre-submission `npx eas-cli@latest build:version:get -p ios` evidence and guard against repeating the App Store Connect build `31` / build version `1.1.1` mismatch.
- Added `scripts/check-release-metadata.mjs`, `npm run check:release`, and `.github/workflows/ci.yml` for smoke CI covering install plus release metadata drift.
- Added `scripts/check-expo-config.mjs`, `npm run check:expo-config`, and a post-`npm ci` CI step that runs Expo config resolution with placeholder public env and verifies production EAS builds fail fast when required public app env is missing.
- Hardened release configuration in the worktree: camera permission copy now truthfully covers pet-food label scans and human-food safety scans, non-development EAS builds fail during app config resolution when required public Supabase/RevenueCat environment variables are missing, and `scripts/check-release-metadata.mjs` now guards native IDs, plugins, permissions, EAS update/runtime settings, and this build-time env validation.
- Added `scripts/check-secrets.mjs`, `npm run check:secrets`, and CI secret scanning before dependency install. Current tracked env-related files are `.env.example`, `app.config.js`, and `config/env.js`; `.env.example` is placeholder-only and `.env` is ignored/not tracked in the current index.
- Added `scripts/check-js-syntax.mjs`, `npm run check:syntax`, and CI JavaScript syntax plus self-referential hook dependency, hard-coded app price, non-zero UI letter-spacing, scan-timeout ordering, structured scan-failure telemetry, RevenueCat purchase/restore diagnostics, purchase/restore entitlement refresh markers, SDK-active RevenueCat entitlement precedence, guest auth entry, support contact loop, and dev-mode release guard checks before dependency install. The guard requires the local scan/paywall bypass to stay tied directly to `__DEV__` and the dev banner to stay behind an `__DEV__` render guard.
- Added `scripts/check-analytics-privacy.mjs`, `npm run check:analytics`, and CI analytics privacy checks. `services/analytics.js` now redacts emails, URLs, file URLs, plain local file paths, JWT-like tokens, common API-key prefixes, and long opaque payloads before local queueing or Supabase insertion. `services/errorReporting.js` now emits normalized `error_category` / `error_fingerprint` fields for production JS error grouping, and the guard requires centralized release context, queued-event ownership checks, boot-time queue flushing, flush/drop diagnostics, safe error grouping, and non-queued sign-out attribution on analytics events.
- Added `scripts/check-app-privacy-disclosure.mjs`, `npm run check:privacy`, and CI App Privacy disclosure checks so App Store Connect privacy-label answers stay aligned with code/data-flow evidence and the hosted privacy page.
- Added `scripts/check-accessibility.mjs`, `npm run check:accessibility`, and CI accessibility label checks. The guard scans app/screen JSX for interactive `Pressable`, `TouchableOpacity`, and `TouchableHighlight` controls with `onPress` and requires explicit `accessibilityRole` and `accessibilityLabel`.
- Added `scripts/check-claim-safety.mjs`, `npm run check:claims`, and CI claim-safety checks over app screens/services, Edge Functions, hosted docs/legal copy, and release config so unsupported DogFoodAdvisor/CatFoodAdvisor, customer-review, recall, veterinary-approval, guaranteed-safety, or medical-diagnosis claims cannot re-enter production surfaces unnoticed.
- Added `scripts/check-app-store-listing.mjs`, `npm run check:listing`, and CI App Store listing checks so the recommended replacement metadata stays within Apple limits and keeps the no-account promise gated on validated guest scanning.
- Added `scripts/check-sql-migrations.mjs`, `npm run check:sql`, and CI SQL migration safety checks before dependency install. The guard checks migration numbering, RLS on created tables, safe `SECURITY DEFINER` search paths, KPI view access revokes/grants, release telemetry KPI coverage, scan-failure KPI coverage, first-run auth entry KPI coverage including auth-screen fallback exposure, analysis-cache health KPI coverage, anonymous function grants, profile write hardening, `scan_history` update-policy coverage, delete-account table coverage and RPC execute permissions, tier-aware Pro rate-limit behavior, reversed scan-id retry handling in `consume_scan`, and protection against authenticated clients overriding the free-scan limit.
- Added `scripts/check-kpi-runbook.mjs`, `npm run check:kpi`, and CI coverage so the weekly review runbook continues to reference the reporting views and dashboard inputs needed for the operating review.
- Added `scripts/check-ci-release-alignment.mjs`, `scripts/release-gates.mjs`, `npm run check:ci`, CI coverage, and release-preflight coverage so every local release gate, including raw commands such as `git diff --check`, appears in `.github/workflows/ci.yml` in the expected order around `npm ci`, and every scripted gate has a package alias.
- Added `scripts/check-deployment-readiness.mjs`, `npm run check:deployment`, and CI coverage so `DEPLOYMENT_CHECKLIST.md` stays aligned with tracked audit migrations, Edge Function deploy commands, required dashboard toggles, Supabase/RevenueCat secrets, key TestFlight validation markers, and CI coverage for Edge Function fingerprinting plus live-verifier dry-run.
- Added `RELEASE_EVIDENCE.md`, `RELEASE_EVIDENCE_RUNBOOK.md`, `scripts/check-release-evidence.mjs`, `npm run check:evidence`, CI coverage, and release-preflight structure coverage so the external proof required before production submission or paid-growth launch has a durable place in the repo. Normal preflight validates the matrix structure and requires a capture recipe for every evidence key; `npm run check:evidence -- --strict` fails until GitHub CI, Supabase deploy/validation, RevenueCat purchase/restore sync, App Store metadata/live listing, EAS build numbering, Sentry release health, TestFlight smoke, KPI ingestion, and growth-spend evidence are filled with non-sensitive proof.
- Added `scripts/check-release-preflight.mjs`, `npm run check:preflight`, and deployment-readiness coverage so local release prep can run the ordered release gates from the shared `scripts/release-gates.mjs` manifest in one command. The script also supports `--dependency-free` for environments without Deno, installed package tooling, or `node_modules`.
- Added `scripts/check-expo-versions.mjs`, `npm run check:expo-versions`, CI coverage, and release-preflight coverage so Expo-managed package patch drift is caught by `expo install --check --json`. Expo-managed packages are now aligned to SDK 55's expected patch versions, including `react-native@0.83.6` and `react-native-worklets@0.7.4`.
- Added `scripts/check-expo-export.mjs`, `npm run check:bundle`, CI coverage, and release-preflight coverage so iOS and Android Hermes bundle export plus an explicit Expo web export catch Metro/import/dependency-tree/web-runtime failures before TestFlight or local browser QA. The web export asserts a browser JavaScript bundle is produced, public Supabase/RevenueCat config is embedded, and the private Sentry auth token is not leaked.
- Added `scripts/check-expo-prebuild.mjs`, `npm run check:prebuild`, CI coverage, and release-preflight coverage so Expo native project generation is tested in a temporary directory. Added `expo-system-ui@~55.0.18` so Android `userInterfaceStyle` native config is actually applied during prebuild.
- Added Sentry native crash reporting configuration in the worktree: `@sentry/react-native` at the Expo SDK 55-compatible `~7.11.0` range, the Expo config plugin, the Sentry Metro wrapper, `SENTRY_DSN` app config, required Sentry EAS env validation, root `Sentry.init`, `Sentry.wrap(App)`, release/build/update tags, `sendDefaultPii: false`, tracing disabled by default, and `beforeSend` diagnostic scrubbing. First-party `app_error_captured` analytics now redacts JWT-like tokens and common secret-key prefixes before truncating diagnostic text, matching the analytics/Sentry scrubber posture. `scripts/check-crash-reporting.mjs`, `npm run check:crash-reporting`, `scripts/check-analytics-privacy.mjs`, `npm run check:analytics`, and CI now enforce the package/config/plugin/Metro/init/privacy contract plus app-error telemetry redaction.
- Added `scripts/check-dependency-audit.mjs`, `npm run check:audit`, and CI coverage after `npm ci` so production dependency audits fail on high/critical advisories, moderate-count regressions, or nested React Native lockfile regressions while the current Expo/React Native moderate baseline is remediated separately.
- Added request-aware Edge Function CORS in the worktree: native/no-Origin requests are allowed, localhost dev browser origins are allowed, and production browser origins must be listed in optional `WOOF_ALLOWED_ORIGINS`. Static wildcard CORS was removed from `analyze`, `product-lookup`, `revenuecat-sync`, and `revenuecat-webhook`.
- Added `scripts/check-edge-functions.mjs`, `npm run check:edge`, and CI Edge Function safety checks before dependency install. The guard checks function registration, preflight/method guards, auth/webhook auth, required env handling, required RPC usage, request-aware CORS, deployment marker headers, and hard-coded secret patterns.
- Added `scripts/check-edge-typecheck.mjs`, `npm run check:edge-types`, committed `deno.lock`, CI Deno setup, and release-preflight coverage so all tracked Supabase Edge Functions type-check under Deno before deploy without remote module drift. `analyze` and `revenuecat-webhook` now use the docs-style `SupabaseClient` helper parameter type instead of narrowing helper table writes to `never`.
- Hardened audit table grants for Supabase's 2026 Data API exposure change: `analytics_events` now explicitly grants authenticated insert only, `scan_usage_events` grants authenticated select only, and `revenuecat_events` remains service-role only; `scripts/check-sql-migrations.mjs` now enforces those grants and public/anon revokes for the new audit tables.
- Added Edge Function deployment reconciliation markers: every tracked function now returns `X-Woof-Function-Name` and `X-Woof-Function-Audit-Version`, and `scripts/fingerprint-edge-functions.mjs` / `npm run edge:fingerprint` prints local SHA-256 fingerprints to record before comparing or deploying Supabase dashboard versions.
- Added `scripts/verify-live-edge-functions.mjs` / `npm run edge:verify-live` so post-deploy validation can call the live Supabase Edge Function OPTIONS endpoints and fail if any function is still serving a stale or unmarked build. The script supports `--dry-run` for local release prep without a configured Supabase host.
- Replaced Edge Function non-null env assertions with explicit `requiredEnv` handling in `analyze`, `product-lookup`, `revenuecat-webhook`, and `revenuecat-sync`.
- Hardened older `SECURITY DEFINER` migrations by adding explicit `SET search_path = public` to the profile trigger, rate-limit RPC, and initial cleanup RPCs.
- Added `services/logger.js` and moved app-side diagnostic logging behind `__DEV__`; raw console calls now remain only in Supabase Edge Functions and the logger implementation.
- Added final-response validation in `services/claude.js` so completed pet-food and human-food Claude results must include the fields the app needs before the UI saves or renders them as trusted scan results.
- Added matching final-response validation in `supabase/functions/analyze/index.ts` before returning non-streaming Claude responses and before writing streaming results to `analysis_cache`.
- Added scan reversal support in the worktree: `063_scan_reversal.sql`, Edge Function reversal calls on downstream Claude/network/validation failures, and failed-analysis scan count sync in `ResultsScreen`.
- Added reversed-scan retry and client-limit hardening in the worktree: `069_consume_scan_reversed_retry.sql` replaces `consume_scan` so reusing a reversed failed `scan_id` must pass the current entitlement check and re-consume a free scan when appropriate, instead of reusing an old `allowed=true,counted=false` event. Because authenticated clients can call `consume_scan` for barcode cache-hit accounting, the RPC now ignores client-provided free-limit overrides unless the caller is `service_role`.
- Added production advisor hardening in the worktree: `070_security_advisor_hardening.sql` fixes the live `is_likely_non_product_catalog_row` mutable search-path finding and removes default `PUBLIC`/`anon` execute privileges from historical app RPCs while preserving authenticated/service-role access where current or legacy app clients may still need it.
- Added `supabase/validation/post_deploy_audit_validation.sql`, a read-only post-deploy SQL artifact that checks audit migration application, required tables/columns/RPCs/views, required KPI view columns including the scan-id-matched upload-cost fields, entitlement column privilege hardening, anon RPC execution hardening, and the `is_likely_non_product_catalog_row` search-path advisor fix.
- Added stream-level final error signaling for malformed streamed responses via a `woof_error` SSE event carrying reversed scan usage.
- Added Edge stream-cancel reversal in the worktree: if the client disconnects before a streamed result is delivered, `analyze` calls `reverse_scan` with `client_stream_cancelled` so a counted free scan is not silently lost. The app now wires intentional loading-screen exits through `analysisService.cancelAnalysis()` from `ResultsScreen`, so tapping Back or leaving the running results route aborts the stream instead of silently letting it consume a free scan in the background.
- Moved photo and human-food scan usage sync to the Edge response path: successful streams now emit `woof_scan_usage`, non-streaming responses include `scanUsage`, and `services/analysisService.js` no longer pre-consumes image scans before upload. `services/claude.js` now refuses to complete a stream without the final `woof_scan_usage` confirmation and retries/falls back instead of trusting optimistic partial JSON. Photo cache hits and completed in-memory photo entries no longer complete a new scan attempt before that scan-usage confirmation arrives; barcode cache hits still consume client-side because they bypass `analyze`.
- Moved app-side analysis timeouts beyond the Edge Function Claude timeout so server-side scan reversal can return before the client/network/UI watchdogs give up locally.
- Fixed cached-scan retention in the worktree: barcode cache hits, photo cache hits, and post-entitlement completed-result fallbacks now save a local replay copy and scan-history entry just like fresh analyses. New scan attempts do not reuse completed in-memory entries until their barcode/client or Edge scan-accounting path has run. History display and Supabase history sync also preserve fallback names for human-food or older local entries.
- Added paid-user scan-limit recovery in the worktree: if `analyze` returns `SCAN_LIMIT_REACHED`, `screens/ResultsScreen/index.js` refreshes RevenueCat/Supabase entitlement once through `refreshProStatus({ source: "scan_limit_recovery" })` and retries the same scan before showing the paywall. Analytics now record `scan_limit_recovery_started`, `scan_limit_recovery_retried`, and failure/no-Pro outcomes.
- Hardened client Pro state in the worktree: `services/auth.js` now checks `profiles.pro_expires_at` before trusting profile-derived `is_pro`, matching the server-side `consume_scan` gate.
- Hardened auth entitlement ordering in the worktree: profile refreshes during app start, RevenueCat sync, and Apple/Google guest account-linking no longer overwrite fresher RevenueCat Pro state; account-linking triggers `refreshProStatus` after profile sync. A self-referential `useCallback` dependency in the Pro-status resolver was removed and guarded against in `check:syntax`, and PKCE OAuth callback exchange support is now guarded there too.
- Added a RevenueCat SDK-unchecked fallback in the worktree: `services/purchases.js` now exposes detailed Pro status, and `services/auth.js` falls back to server subscriber sync and then an active unexpired Supabase profile entitlement when customer info cannot be checked. Fallback use is tracked through `revenuecat_status_fallback_used`.
- Added a product UI accessibility/readability pass across onboarding, auth, home, scanner, results, paywall, profile, and legal WebView screens: primary controls now expose clearer roles, labels, hints, selected/disabled states, and scan-result context for VoiceOver/TalkBack; UI `letterSpacing` values are normalized to `0` in shared typography and screen styles.
- Added `DEPLOYMENT_CHECKLIST.md` with the production migration, Edge Function, RevenueCat webhook, analytics, TestFlight, and rollback gates needed to ship the audit work safely.

## Verification Done

- `git diff --check`
- `PATH="/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" /Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node /tmp/codex-npm-cli/package/bin/npm-cli.js ci`
- `PATH="/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" DENO_BIN=/tmp/woof-deno/bin/deno NPM_CLI_JS=/tmp/codex-npm-cli/package/bin/npm-cli.js /Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-release-preflight.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-release-preflight.mjs --dependency-free`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-secrets.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-js-syntax.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-analytics-privacy.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-app-privacy-disclosure.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-accessibility.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-claim-safety.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-app-store-listing.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-revenuecat-readiness.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-sql-migrations.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-deployment-readiness.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-crash-reporting.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-edge-functions.mjs`
- `DENO_BIN=/tmp/woof-deno/bin/deno /Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-edge-typecheck.mjs`
- `/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-release-metadata.mjs`
- `NPM_CLI_JS=/tmp/codex-npm-cli/package/bin/npm-cli.js /Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-dependency-audit.mjs`
- `PATH="/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" /Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-expo-versions.mjs`
- `PATH="/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" /Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-expo-config.mjs`
- `PATH="/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" /Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-expo-export.mjs`
- `PATH="/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:$PATH" /Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/check-expo-prebuild.mjs`
- Bundled Node syntax checks:
  - `node --check scripts/check-secrets.mjs`
  - `node --check scripts/check-js-syntax.mjs`
  - `node --check scripts/check-analytics-privacy.mjs`
  - `node --check scripts/check-app-privacy-disclosure.mjs`
  - `node --check scripts/check-accessibility.mjs`
  - `node --check scripts/check-claim-safety.mjs`
  - `node --check scripts/check-app-store-listing.mjs`
  - `node --check scripts/check-sql-migrations.mjs`
  - `node --check scripts/check-deployment-readiness.mjs`
  - `node --check scripts/check-edge-functions.mjs`
  - `node --check scripts/check-edge-typecheck.mjs`
  - `node --check scripts/check-release-metadata.mjs`
  - `node --check app.config.js`
  - `node --check App.js`
  - `node --check services/analytics.js`
  - `node --check services/logger.js`
  - `node --check services/entitlements.js`
  - `node --check services/auth.js`
  - `node --check services/claude.js`
  - `node --check services/history.js`
  - `node --check services/analysisService.js`
  - `node --check services/opff.js`
  - `node --check services/purchases.js`
  - `node --check services/revenuecatSync.js`
  - `node --check screens/OnboardingScreen.js`
  - `node --check screens/AuthScreen.js`
  - `node --check screens/HomeScreen.js`
  - `node --check screens/ScannerScreen.js`
  - `node --check screens/ResultsScreen/index.js`
  - `node --check screens/ResultsScreen/components.js`
  - `node --check screens/ResultsScreen/styles.js`
  - `node --check screens/PaywallScreen.js`
  - `node --check screens/ProfileScreen.js`
  - `node --check legal.js`

Dependencies were installed locally through the temporary npm CLI plus bundled Node, the native Metro/Hermes bundle export and explicit Expo web export smoke tests pass, Expo prebuild generation passes in a temp directory, Edge Function Deno type-checking passes through a temporary Deno install, and the full release preflight now passes. Local Expo web browser QA verified the development-only paywall preview/debug route. Full native app build/test and native device/simulator QA were not run locally. SQL migration dry-runs were not run because `psql` and the Supabase CLI are not available on PATH.

Dependency audit status: after deduping React Native for Metro export and adding `expo-system-ui`, `react-dom`, and `react-native-web`, production audit reports `53` moderate advisories and `0` high/critical. Remaining findings are Expo/React Native transitive advisories that need a dedicated upgrade branch and native/TestFlight validation instead of `npm audit fix --force`.

## Highest Next Actions

1. Resolve Supabase egress overage or upgrade before July 15, 2026, then validate `analysis_upload_started` / `kpi_daily_funnel` upload-byte metrics after deployment.
2. Enable Supabase Anonymous Sign-Ins and manual identity linking, record the anonymous-user RLS/abuse/cleanup and Leaked Password Protection decisions from `DEPLOYMENT_CHECKLIST.md`, then validate guest scan, local history preservation, and Apple/Google account-saving in TestFlight.
3. Use `SUPABASE_LIVE_RECONCILIATION.md` and `DEPLOYMENT_CHECKLIST.md` to resolve Supabase migration-history drift, then deploy migrations `058_human_food_history.sql` through `070_security_advisor_hardening.sql`; deploy updated `analyze`, `product-lookup`, `revenuecat-webhook`, and new `revenuecat-sync` functions; validate KPI views, profile write restrictions, delete-account cleanup, product lookup, free-scan enforcement, image scan-usage sync, scan reversal/reversed-scan retry hardening, tier-aware rate limiting, cleanup runs, advisor hardening, paywall trial-eligibility copy, RevenueCat webhook entitlement sync, and immediate purchase/restore profile sync in TestFlight.
4. Use `APP_STORE_CONNECT_AUDIT.md`, `APP_PRIVACY_DISCLOSURE.md`, and App Store Connect to publish/update App Privacy answers, remove unsupported source/recall/overbroad safety claims from the 1.2 Distribution record, verify the build/version row, save Product Page Preview evidence, and run `npm run check:live-listing -- --guest-validated` after the metadata is publicly visible.
5. Use `APP_STORE_LISTING.md` to add Promotional Text, replace the App Store description/keywords/review notes, and refresh screenshots/copy after guest scanning is validated.
6. Run `npm run check:github-release` and use `GITHUB_RELEASE_AUDIT.md` before publishing: do not merge draft PR #1 as-is; reconcile or supersede `codex/push-woof-app`, remove the stale `supabase/migrations/007`-`049` files that collide with production's `007`-`057` history, push the current audit work with `.github/workflows/ci.yml` and `.github/pull_request_template.md`, and require the new CI checks to run green on GitHub before merge.
7. Run `npm run edge:fingerprint`, deploy tracked Edge Functions, run `npm run edge:verify-live` against the Supabase functions host, and compare dashboard source only if live headers or behavior do not match the tracked build.
8. Validate event inserts and run the first weekly operating review from `WEEKLY_REVIEW_RUNBOOK.md` using `KPI_FRAMEWORK.md` / `065_kpi_reporting_views.sql`.
9. Use `GROWTH_CREATIVE_PLAN.md` only after the release gates are satisfied: start with App Store Search Ads and small static/social creative tests optimized for first completed scan, not installs.
10. Run a TestFlight VoiceOver/TalkBack smoke pass through guest start, scan, human-food pet picker, result, paywall plan selection, restore/legal links, profile save-account, and delete confirmations.
11. Use `EAS_RELEASE_VERSIONING.md`, run `npm run check:eas-versioning`, and save `npx eas-cli@latest build:version:get -p ios` evidence before the next App Store submission.
12. Configure Sentry EAS secrets, validate Sentry source-map upload and crash-free sessions plus first-party `app_error_captured` category/fingerprint grouping, and validate stream-level final error signaling in TestFlight.
13. Fill `RELEASE_EVIDENCE.md` with non-sensitive proof from the external release checks, then run `npm run check:evidence -- --strict` before production submission or paid-growth launch.
14. Create a dependency remediation branch for the remaining `53` moderate npm advisories, run `npm ci`, Expo config resolution, bundle export, prebuild, native build, and TestFlight smoke validation before accepting major Expo/React Native-related package moves.
15. Expand CI with app lint/typecheck, native build/runtime smoke checks, and Supabase migration dry-runs once package/native/database tooling is available.
