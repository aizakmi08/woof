# Woof Release Evidence

Last updated: 2026-07-16.

Purpose: keep the final production-release proof in the repo, separate from chat history. Do not paste private keys, service-role tokens, customer PII, App Store Connect private account fields, or RevenueCat secrets into this file. Use links, screenshots, command names, timestamps, and short non-sensitive notes.

Run the structural check any time this file changes:

```sh
npm run check:evidence
```

Use `RELEASE_EVIDENCE_RUNBOOK.md` to collect proof for each matrix row before changing a status to `Ready` or `Waived`.

Before submitting a production build or scaling paid acquisition, replace the pending evidence below and run:

```sh
npm run check:evidence -- --strict
```

## Evidence Matrix

| Key | Status | Required Evidence | Evidence Link Or Note |
| --- | --- | --- | --- |
| github_current_branch_ci | Ready | Current release branch is pushed, the superseding PR is open, and GitHub CI from `.github/workflows/ci.yml` is green; stale PR #1 remains draft and is not the release line. | 2026-07-16: [PR #3](https://github.com/aizakmi08/woof/pull/3), branch `codex/production-release-20260716`, commit `24737ed9`; [green CI run](https://github.com/aizakmi08/woof/actions/runs/29490215097). |
| supabase_migration_history | Ready | `supabase migration list` evidence shows production history through `057` plus local audit migrations `058+` are intentionally reconciled before push. | 2026-07-16 Supabase connector confirmed production history through `301_normalize_wellness_stew_identity`, including the July 15-16 search, history-image, and catalog identity fixes. |
| supabase_migrations_applied | Ready | Migrations `058` through `070` applied in production and `supabase/validation/post_deploy_audit_validation.sql` returns only `pass = true` rows. | 2026-07-16 live project is `ACTIVE_HEALTHY`; all six deployed Edge Functions are active. The read-only catalog contract currently classifies `8,297/8,297` strict ready rows consistently, with `4,947` dog and `3,350` cat rows. |
| supabase_edge_functions_live | Ready | `analyze`, `product-lookup`, `revenuecat-webhook`, and `revenuecat-sync` are deployed; `npm run edge:verify-live` passes against the live functions host. | 2026-07-10 `npm run edge:verify-live` passed `5/5`: `analyze`, `label-lookup`, `product-lookup`, `revenuecat-sync`, and `revenuecat-webhook` returned the expected names and audit versions. Deno 2.7.0 also passed the frozen-lockfile type check for all five functions. |
| supabase_auth_dashboard | Ready | Anonymous Sign-Ins, manual identity linking, Apple/Google redirect URLs, anonymous-user RLS implications, abuse controls, cleanup retention, and Leaked Password Protection decision are recorded. | 2026-07-10 production Auth now has Anonymous Sign-Ins and manual linking enabled; `npm run auth:verify-live` passes with signup, anonymous, Apple, and Google enabled. A clean-install iPhone 17e cold start created anonymous auth user `166bbc99...`, verified catalog search returned three Open Farm GoodGut variants, and opening the exact verified result persisted `profiles.scan_count=1`, one history row, one usage row, and analytics under the guest user. The one-time save-history prompt launched the Google identity-linking handoff and cancellation returned to the intact guest result. Abuse decision: keep Supabase's conservative anonymous Auth rate limit plus Woof's 20/hour free scan throttle, monitor guest-auth failure/growth KPIs, and add mobile CAPTCHA/Turnstile if abuse appears. Retention decision: retain dormant anonymous accounts for 30 days, but do not enable destructive cleanup until cascade, active-entitlement, product-submission, and account-link preservation tests pass. Leaked Password Protection is waived while the app exposes only OAuth and anonymous auth, with no password sign-in surface. |
| supabase_egress_plan | Pending | Supabase egress overage is resolved or the plan is upgraded before the July 15, 2026 grace-period risk; post-deploy upload-byte KPIs are reviewed. | TODO: Add billing screenshot/link and KPI query timestamp. |
| revenuecat_offering_packages | Pending | RevenueCat `default` offering returns weekly, monthly, and annual packages in TestFlight with current store prices/trial eligibility. | 2026-07-10 native iOS Simulator dev build loaded weekly `$4.99`, monthly `$7.99`, and annual `$29.99` packages with three-day trial copy. Direct Xcode launch exposed and fixed an invalid generated StoreKit scheme reference; the corrected `../woof/Woof.storekit` path is now enforced by `check:revenuecat` and a clean temporary Expo prebuild. Xcode completed the no-charge local `woof_pro_monthly` purchase and RevenueCat accepted the Xcode receipt. TestFlight proof is still required. |
| revenuecat_webhook_sync | Pending | RevenueCat webhook test event and at least one sandbox purchase/restore sync through `revenuecat-webhook` and `revenuecat-sync` without repeated `subscriber_sync_error`. | 2026-07-10 local purchase and restore each invoked `revenuecat-sync` successfully and emitted `revenuecat_profile_sync_completed`. RevenueCat accepted both Xcode-environment receipt posts, while its server subscriber API remained free for this local StoreKit transaction; the app recorded `revenuecat_status_mismatch` and retained the SDK-confirmed local entitlement. A clean runtime test then scheduled one bounded reconciliation, made exactly three authenticated backoff attempts, received `200` for each function call, emitted started/attempt/exhausted diagnostics, and correctly left Supabase free rather than promoting a client claim. A RevenueCat dashboard webhook test and TestFlight sandbox event are still required. |
| revenuecat_purchase_restore | Pending | Sandbox purchase, restore, cancellation, and expiration update RevenueCat, Supabase `profiles.is_pro`, app entitlement state, and analytics diagnostics. | 2026-07-10 direct-Xcode StoreKit testing completed a no-charge `woof_pro_monthly` purchase, restore, cancellation, and local entitlement revocation. The guarded StoreKitTest cleanup now accelerates renewals, expires every purchased Woof subscription product, clears local transactions, and passed on the iOS Simulator. After cleanup, Home showed the free-scan status and Profile showed `Upgrade to Woof Pro` / `Free plan`; Supabase remained `is_pro=false`, `pro_expires_at=null`, with no RevenueCat product or entitlement IDs. Screenshots are stored at `outputs/simulator/storekit-post-expiry-free-profile-2026-07-10.jpg` and `outputs/simulator/storekit-post-expiry-restore-no-purchases-2026-07-10.jpg`. TestFlight purchase/restore/cancel/expiration proof is still required. |
| app_store_privacy | Pending | App Store Connect App Privacy answers match `APP_PRIVACY_DISCLOSURE.md`, including no tracking/IDFA and Sentry Crash Data status. | TODO: Add App Store Connect timestamp or screenshot link. |
| app_store_metadata | Pending | App Store Connect metadata removes unsupported DogFoodAdvisor/CatFoodAdvisor, recall-alert/history, customer-review, guaranteed-safety, and overbroad medical claims. | TODO: Add Product Page Preview timestamp or screenshot link. |
| app_store_live_listing | Pending | `npm run check:live-listing -- --guest-validated` passes after the updated public US listing is visible. | TODO: Add command output timestamp. |
| eas_remote_versioning | Ready | `npm run check:eas-versioning` passes and `npx eas-cli@latest build:version:get -p ios` proves the next iOS build number is greater than App Store Connect build `31`. | 2026-07-16: `npm run check:eas-versioning` passed; EAS reported remote iOS build number `40`, then production build `41` completed successfully for version `1.2.0` using the App Store distribution profile. The signed artifact and submission record are linked from the TestFlight handoff. |
| sentry_release_health | Pending | Sentry EAS secrets are configured; source-map upload succeeds; TestFlight crash-free sessions and first-party `app_error_captured` grouping are reviewed. | Code and EAS integration are present; Sentry release-health dashboard evidence is still required. |
| testflight_guest_scan | Pending | TestFlight validates first-run guest scan, free-scan enforcement, scan reversal/retry behavior, local history preservation, and Apple/Google account saving. | 2026-07-10 iOS Simulator regression passed verified typo search, no-match acquisition, 42-item exact ingredient expansion, personalized avoid-ingredient warnings, history replay/recovery, distinct-product comparison, scan-limit paywall, human-food/ingredient/label retry recovery, and quota reversal. Supabase/Edge logs on 2026-07-16 show successful `label-lookup` and `analyze` requests, but physical-camera and TestFlight proof are still required. |
| testflight_accessibility_smoke | Pending | VoiceOver/TalkBack smoke pass covers guest start, scanner, result, paywall plan selection, restore/legal links, profile account-save, and delete confirmation. | TODO: Add device/build/run notes. |
| kpi_event_ingestion | Pending | Supabase analytics/KPI views show events for onboarding, scan start/completion/failure, paywall request/view/package load/purchase/restore, support, review, sharing, and app errors. | 2026-07-10 queued simulator analytics flushed into `analytics_events`, KPI views are live, and scan failure/reversal events were observed. The native purchase test also persisted paywall request/view/package-load, purchase start/completion, entitlement refresh, restore start/completion, restore entitlement refresh, profile-sync completion, and status-mismatch diagnostics. A complete TestFlight event-family readout is still required. |
| growth_spend_gate | Pending | Paid acquisition remains paused until the above evidence is ready; first Search Ads/social tests use `GROWTH_CREATIVE_PLAN.md` and optimize for first completed scan. | TODO: Add launch decision note. |

## Live Catalog Snapshot

As of 2026-07-16, the live project contains `17,694` product rows and `13,123` known dog/cat rows. The strict source-backed quality function reports `8,297` `verified_ready` rows (`4,947` dog and `3,350` cat), `1,143` `needs_ingredients`, `914` `identity_only`, and `7,340` `excluded`. There are `4,788` open/in-progress acquisition gaps affecting `7,866` products. This is a verified gap backlog, not permission to promote unverified or duplicate rows; complete US-market coverage remains pending.

## Strict Mode Rules

`npm run check:evidence -- --strict` requires every matrix row to be `Ready` or `Waived`, and every evidence note must be replaced with concrete non-sensitive proof. Use `Waived` only with a clear rationale and owner/date in the evidence note.
